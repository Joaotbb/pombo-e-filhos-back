# Server Configuration
PORT= [Port number for the server, e.g., 3000]

# Database Configuration
DB_HOST= [Database host URL, e.g., localhost or a remote database URL]
DB_USER= [Database username, e.g., root or your database username]
DB_PASSWORD= [Database password, e.g., password123]
DB_DATABASE= [Database name, e.g., myapp_db]
DB_PORT= [Database port]

# MySQL URI (Complete MySQL connection string if used)
MYSQL_URI= [Complete MySQL URI, e.g., mysql://user:password@host:port/database]

# JWT Secret Key (Used for signing and verifying JWT tokens)
JWT_ACCESS_TOKEN= [Your secret key for JWT, e.g., mysupersecretkey]

# Shadow Database URL (Optional for Prisma migrations in some configurations)
SHADOW_DATABASE_URL= [Shadow database URL if used, similar to MYSQL_URI]

# Node Environment (Typically development or production)
NODE_ENV= [Node environment, e.g., development or production]